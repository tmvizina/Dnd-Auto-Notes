/**
 * The bridge is intentionally empty until the IPC contract ticket lands.
 * Keeping this as a real bundled entry point makes the preload boundary
 * explicit without exposing Electron or Node objects to the renderer.
 */
export {};
